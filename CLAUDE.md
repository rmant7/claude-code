# User preferences

- Never ask clarifying questions as a multiple-choice / bulleted list of options (including via the AskUserQuestion tool). The user has observed this causing sessions to hang. Instead, ask a single plain-text question in normal prose.
